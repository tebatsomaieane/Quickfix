# Base URL for API requests.
# Leave unset (/api) when the API is served from the same origin.
# On Cloudflare Pages set this to the API server origin, e.g.
#   VITE_API_URL=https://api.quickfix.co.ls
VITE_API_URL=/api