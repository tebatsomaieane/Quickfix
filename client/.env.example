# Base URL for API requests. Leave unset in production so the app talks to
# the same origin (the reverse proxy forwards /api to the server).
VITE_API_URL=/api

# Show "demo account" quick-login buttons on the login screen.
# Defaults: true in development, false in production builds.
VITE_SHOW_DEMO=false